# 🚨 JavaScriptファイルがアップロードされていない場合

## 解決方法

### すぐに実行してください：

```bash
# jsフォルダごとアップロード
git add js/
git commit -m "📦 JavaScriptファイル追加"
git push origin main
```

### または全ファイルをアップロード

```bash
git add .
git commit -m "📦 全ファイルアップロード"
git push origin main
```

### 2〜3分待ってから再確認

```
https://komono2nd-hub.github.io/shopsite-demo/debug.html
```

---

## 📂 必要なファイル一覧

以下のファイルがGitHubにアップロードされている必要があります：

```
shopsite-demo/
├── js/
│   ├── common.js    ← 必須
│   ├── auth.js      ← 必須
│   └── cart.js      ← 必須
├── css/
│   ├── common.css
│   ├── auth.css
│   └── (その他のCSS)
├── index.html
├── login.html
├── mypage.html
├── seller-dashboard.html
└── debug.html
```

---

## ✅ 確認方法

### GitHubで確認

```
1. https://github.com/komono2nd-hub/shopsite-demo に移動
2. jsフォルダをクリック
3. common.js, auth.js, cart.js が表示されるか確認
```

### ローカルで確認

```bash
# ファイルが存在するか確認
ls -la js/

# 以下が表示されればOK:
# common.js
# auth.js  
# cart.js
```

---

## 🔧 トラブルシューティング

### ケース1: .gitignore でjsフォルダが除外されている

```bash
# .gitignore を確認
cat .gitignore

# もし js/ が含まれていたら削除
```

### ケース2: ファイルがステージングされていない

```bash
# 状態確認
git status

# jsフォルダを強制追加
git add -f js/
git commit -m "JSファイル追加"
git push origin main
```

### ケース3: 大文字小文字の問題

```bash
# 正しいパスを確認
ls js/

# ファイル名が正確か確認（大文字小文字も含めて）
```

---

## 📊 現在の状態確認コマンド

```bash
# リポジトリのファイル一覧
git ls-files

# js/ で始まる行があるか確認
git ls-files | grep "^js/"

# 以下が表示されるべき:
# js/common.js
# js/auth.js
# js/cart.js
```
